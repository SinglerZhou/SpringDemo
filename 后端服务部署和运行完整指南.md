
### 后端服务部署和运行完整指南

#### 第一步：确认项目结构
您的项目已经包含了所有必要的文件：
✅ Application.java - Spring Boot启动类 </br>
✅ UserController.java - 控制器（提供API接口）</br>
✅ UserService.java - 业务逻辑层</br>
✅ RegisterRequest.java 和 LoginRequest.java - 请求DTO</br>
✅ Result.java - 统一响应格式</br>
✅ pom.xml - Maven配置（已包含Spring Boot依赖）</br>
#### 第二步：在IntelliJ IDEA中运行（推荐方式）</br>
##### 方法1：直接运行Application类（最简单）
打开项目 </br>
在IntelliJ IDEA中打开</br>
` C:\Users\zhouxingui\IdeaProjects\SpringDemo 文件夹`
找到启动类 </br>
在项目树中找到 `src/main/java/org/example/Application.java`</br>
运行应用</br>
右键点击 Application.java 文件中的 main 方法</br>
选择 "Run 'Application.main()'"</br>
或者点击左侧的绿色三角形图标 ▶️</br>
查看控制台输出</br>
等待几秒钟，您应该看到类似这样的输出：</br>
`Tomcat started on port(s): 8080 (http) with context path ''
Started Application in X.XXX seconds
===================================
应用启动成功!
注册接口: POST http://localhost:8080/api/user/register
登录接口: POST http://localhost:8080/api/user/login
===================================`
</br>
验证服务是否启动成功
打开浏览器访问：http://localhost:8080/
如果看到 "Whitelabel Error Page" 是正常的（因为没有配置首页）
这表示Tomcat服务器已经在运行了

##### 方法2：使用Maven命令运行
如果您更喜欢命令行方式：
打开PowerShell或命令提示符

##### 进入项目目录

cd C:\Users\zhouxingui\IdeaProjects\SpringDemo

编译并运行
先编译项目</br>
`mvn clean compile`</br>
然后运行</br>
mvn spring-boot:run

第三步：测试API接口
服务启动后，您需要使用正确的方式测试POST接口（不能直接在浏览器地址栏访问）：
方法1：使用HTML测试页面（最适合前端开发者）
我注意到您已经有一个测试HTML文件。让我创建一个更完善的版本：
（根目录下的test.html）

使用方法：
确保后端服务已启动
在浏览器中访问：http://localhost:8080/test.html
填写表单并点击按钮测试

方法2：使用Postman（专业API测试工具）
下载并安装 Postman
创建新请求：
方法：POST
URL：http://localhost:8080/api/user/register
Headers：添加 Content-Type: application/json
Body：选择 "raw" -> "JSON"，输入测试数据
方法3：在前端项目中调用
在您自己的前端项目中，可以这样调用：

// 注册用户
async function registerUser() {
try {
const response = await fetch('http://localhost:8080/api/user/register', {
method: 'POST',
headers: {
'Content-Type': 'application/json',
},
body: JSON.stringify({
name: '张三',
email: 'zhangsan@example.com',
password: '123456',
phone: '13800138000'
})
});

        const result = await response.json();
        console.log('注册结果:', result);
        
        if (result.code === 200) {
            alert('注册成功！');
        } else {
            alert('注册失败: ' + result.message);
        }
    } catch (error) {
        console.error('请求错误:', error);
        alert('网络错误，请检查后端服务是否启动');
    }
}

// 用户登录
async function loginUser() {
try {
const response = await fetch('http://localhost:8080/api/user/login', {
method: 'POST',
headers: {
'Content-Type': 'application/json',
},
body: JSON.stringify({
email: 'zhangsan@example.com',
password: '123456'
})
});

        const result = await response.json();
        console.log('登录结果:', result);
        
        if (result.code === 200) {
            alert('登录成功！用户信息:', result.data);
            // 保存用户信息到localStorage
            localStorage.setItem('userInfo', JSON.stringify(result.data));
        } else {
            alert('登录失败: ' + result.message);
        }
    } catch (error) {
        console.error('请求错误:', error);
        alert('网络错误，请检查后端服务是否启动');
    }
}

第四步：常见问题解决
问题1：端口被占用
如果看到错误 "Port 8080 was already in use"
解决方案： 在 src/main/resources/application.properties 中添加：
server.port=8081

问题2：Maven依赖下载失败
解决方案：
清理并重新下载依赖
mvn clean install -U

问题3：Java版本不匹配
解决方案： 确保使用Java 11：
java -version

问题4：跨域问题（前端调用时）
已在 UserController 中添加 @CrossOrigin(origins = "*")，允许所有域名访问。
如果需要限制特定域名：
Java 代码：
@CrossOrigin(origins = "http://localhost:3000") // 只允许前端开发服务器

第五步：打包部署（可选）
如果您需要将应用部署到服务器：
1. 打包成可执行JAR
mvn clean package

2. 生成的JAR文件位于 target/SpringDemo-1.0-SNAPSHOT.jar

3. 在任何有Java环境的服务器上运行
java -jar target/SpringDemo-1.0-SNAPSHOT.jar
📌 快速开始总结
最简单的运行步骤：
✅ 在IntelliJ IDEA中打开项目
✅ 找到 Application.java
✅ 右键点击 main 方法 → Run
✅ 等待看到 "应用启动成功!"
✅ 浏览器访问 http://localhost:8080/test.html 测试
✅ 在前端项目中调用API接口
API接口地址：
注册：POST http://localhost:8080/api/user/register
登录：POST http://localhost:8080/api/user/login
重要提醒：
⚠️ 必须使用 POST 方法，不能用GET
⚠️ 必须设置 Content-Type: application/json
⚠️ 请求体必须是 JSON格式
⚠️ 不要在浏览器地址栏直接访问POST接口
现在您可以按照这些步骤操作了！如果遇到任何问题，请告诉我具体的错误信息，我会帮您解决。